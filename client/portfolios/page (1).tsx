"use client"

import { useEffect, useRef, useState, type FormEvent, type ReactNode } from "react"
import Link from "next/link"
import { motion, useScroll, useSpring } from "framer-motion"
import { ArrowRight, ArrowUpRight, Github, Linkedin, Mail, Menu, Send, X } from "lucide-react"

const navItems = [
  ["About", "#about"],
  ["Skills", "#skills"],
  ["Projects", "#projects"],
  ["Experience", "#experience"],
  ["Contact", "#contact"],
]

const experiences: [string, string, string, string][] = [
  ["Senior Frontend Engineer", "Tech Innovations Inc.", "2021 - Present", "Lead the frontend development team in building a SaaS platform. Implemented new features, improved performance, and mentored junior developers."],
  ["Frontend Developer", "Digital Solutions Co.", "2019 - 2021", "Developed responsive web applications using React and TypeScript. Collaborated with designers and backend engineers to deliver high-quality products."],
  ["Web Developer", "Creative Agency", "2017 - 2019", "Built websites and web applications for various clients. Worked with HTML, CSS, JavaScript, and WordPress."],
  ["Intern", "Startup Hub", "2016 - 2017", "Assisted in developing web applications and learned modern web development practices."],
]

const skills = [["JavaScript", 90], ["TypeScript", 85], ["React", 95], ["Next.js", 90], ["Node.js", 80], ["HTML/CSS", 95], ["Tailwind CSS", 90], ["GraphQL", 75], ["PostgreSQL", 70], ["AWS", 65], ["Docker", 60], ["Git", 85]]
type Project = [string, string, string[]]
const projects: Project[] = [
  ["E-commerce Platform", "A full-stack e-commerce platform built with Next.js, Stripe, and Prisma.", ["Next.js", "TypeScript", "Prisma", "Stripe"]],
  ["Task Management App", "A collaborative task management application with real-time updates.", ["React", "Firebase", "Tailwind CSS", "Redux"]],
  ["AI Content Generator", "An AI-powered content generation tool using OpenAI's GPT models.", ["Next.js", "OpenAI API", "Node.js", "MongoDB"]],
  ["Fitness Tracker", "A mobile-first fitness tracking application with data visualization.", ["React Native", "TypeScript", "D3.js", "Firebase"]],
  ["Weather Dashboard", "A beautiful weather dashboard with forecasts and historical data.", ["React", "Weather API", "Chart.js", "Styled Components"]],
  ["Portfolio Website", "This portfolio website built with Next.js and Tailwind CSS.", ["Next.js", "Tailwind CSS", "Framer Motion", "TypeScript"]],
]

function Reveal({ children, className = "" }: { children: ReactNode; className?: string }) {
  return <motion.div className={className} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 0.5 }} viewport={{ once: true }}>{children}</motion.div>
}

function Glass({ children }: { children: ReactNode }) {
  return <motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} whileHover={{ y: -5 }} transition={{ duration: 0.5 }} viewport={{ once: true }}><div className="relative overflow-hidden rounded-xl bg-zinc-800/50 backdrop-blur-sm border border-zinc-700/50 p-6 transition hover:border-purple-500/50"><div className="absolute -inset-1 rounded-xl bg-gradient-to-r from-purple-500/10 to-pink-500/10 blur opacity-25" /><div className="relative">{children}</div></div></motion.div>
}

function Heading({ title, subtitle }: { title: string; subtitle: string }) {
  return <div className="text-center space-y-4"><Reveal><span className="inline-block rounded-full border border-white/20 bg-white/10 px-3 py-1 text-sm">{subtitle}</span></Reveal><motion.h2 className="text-4xl md:text-5xl font-bold bg-gradient-to-r from-white to-zinc-300 bg-clip-text text-transparent" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }}>{title}</motion.h2><motion.div className="mx-auto h-1.5 w-24 rounded-full bg-gradient-to-r from-purple-500 to-pink-500" initial={{ scale: 0 }} whileInView={{ scale: 1 }} viewport={{ once: true }} /></div>
}

function MouseFollower() {
  const [position, setPosition] = useState({ x: 0, y: 0 }); const [visible, setVisible] = useState(false)
  useEffect(() => { const move = (e: MouseEvent) => { setPosition({ x: e.clientX, y: e.clientY }); setVisible(true) }; const leave = () => setVisible(false); window.addEventListener("mousemove", move); document.body.addEventListener("mouseleave", leave); return () => { window.removeEventListener("mousemove", move); document.body.removeEventListener("mouseleave", leave) } }, [])
  return <><motion.div className="pointer-events-none fixed left-0 top-0 z-50 h-8 w-8 rounded-full mix-blend-difference" animate={{ x: position.x - 16, y: position.y - 16, opacity: visible ? 1 : 0 }} transition={{ type: "spring", damping: 20, stiffness: 300 }}><div className="h-full w-full rounded-full bg-white opacity-50" /></motion.div><motion.div className="pointer-events-none fixed left-0 top-0 z-50 h-2 w-2 rounded-full bg-white" animate={{ x: position.x - 1, y: position.y - 1, opacity: visible ? 1 : 0 }} /></>
}

function ScrollProgress() { const { scrollYProgress } = useScroll(); const scaleX = useSpring(scrollYProgress, { stiffness: 100, damping: 30 }); const [visible, setVisible] = useState(false); useEffect(() => { const f = () => setVisible(scrollY > 100); window.addEventListener("scroll", f); return () => window.removeEventListener("scroll", f) }, []); return <motion.div className="fixed left-0 right-0 top-0 z-50 h-1 origin-left bg-gradient-to-r from-purple-500 to-pink-500" style={{ scaleX, opacity: visible ? 1 : 0 }} /> }

function ParticleHero() { const ref = useRef<HTMLCanvasElement>(null); useEffect(() => { const canvas = ref.current; const ctx = canvas?.getContext("2d"); if (!canvas || !ctx) return; let frame = 0; let particles: { x: number; y: number; bx: number; by: number; size: number; color: string }[] = []; let mouse = { x: -999, y: -999 }; const resize = () => { const r = canvas.getBoundingClientRect(); const dpr = window.devicePixelRatio || 1; canvas.width = r.width * dpr; canvas.height = r.height * dpr; ctx.setTransform(dpr, 0, 0, dpr, 0, 0); particles = []; for (let y = 15; y < r.height; y += 30) for (let x = 15; x < r.width; x += 30) particles.push({ x, y, bx: x, by: y, size: Math.random() * 4 + 2, color: `hsl(${270 + Math.random() * 60}, 70%, 60%)` }) }; const move = (e: MouseEvent) => { const r = canvas.getBoundingClientRect(); mouse = { x: e.clientX - r.left, y: e.clientY - r.top } }; const animate = () => { const r = canvas.getBoundingClientRect(); ctx.clearRect(0, 0, r.width, r.height); particles.forEach((p, i) => { const dx = mouse.x - p.x, dy = mouse.y - p.y, d = Math.hypot(dx, dy); if (d < 110) { p.x -= dx / d * (110 - d) / 110 * 2; p.y -= dy / d * (110 - d) / 110 * 2 } else { p.x += (p.bx - p.x) / 10; p.y += (p.by - p.y) / 10 } ctx.fillStyle = p.color; ctx.beginPath(); ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2); ctx.fill(); if (i > 0 && Math.hypot(p.x - particles[i - 1].x, p.y - particles[i - 1].y) < 34) { ctx.strokeStyle = "rgba(180,120,255,.2)"; ctx.beginPath(); ctx.moveTo(p.x, p.y); ctx.lineTo(particles[i - 1].x, particles[i - 1].y); ctx.stroke() } }); frame = requestAnimationFrame(animate) }; resize(); animate(); window.addEventListener("resize", resize); window.addEventListener("mousemove", move); return () => { cancelAnimationFrame(frame); window.removeEventListener("resize", resize); window.removeEventListener("mousemove", move) } }, []); return <motion.canvas ref={ref} className="h-[400px] w-full md:h-[500px]" initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 1 }} /> }

function FloatingNav() { const [show, setShow] = useState(false); const [open, setOpen] = useState(false); useEffect(() => { const f = () => setShow(scrollY > 100); window.addEventListener("scroll", f); return () => window.removeEventListener("scroll", f) }, []); return <><motion.nav className={`fixed left-1/2 top-6 z-50 -translate-x-1/2 rounded-full border border-zinc-700/50 bg-zinc-800/80 px-4 py-3 shadow-lg backdrop-blur-md ${show ? "opacity-100" : "pointer-events-none opacity-0"}`} animate={{ y: show ? 0 : -100 }}><div className="flex items-center gap-1"><Link href="/" className="mr-4 text-lg font-bold"><span className="bg-gradient-to-r from-purple-400 to-pink-600 bg-clip-text text-transparent">Shine</span><span>KKA</span></Link><div className="hidden items-center gap-1 md:flex">{navItems.map(([name, href]) => <Link key={name} href={href} className="px-3 py-1 text-sm text-zinc-400 transition hover:text-white">{name}</Link>)}<a href="#contact" className="ml-2 rounded-md bg-gradient-to-r from-purple-500 to-pink-500 px-3 py-2 text-sm font-medium">Resume</a></div><button className="md:hidden" onClick={() => setOpen(!open)} aria-label="Toggle menu">{open ? <X /> : <Menu />}</button></div></motion.nav>{open && <div className="fixed inset-0 z-40 flex flex-col items-center justify-center gap-5 bg-black/95 md:hidden">{navItems.map(([name, href]) => <Link key={name} href={href} onClick={() => setOpen(false)} className="text-2xl">{name}</Link>)}</div>}</> }

function ProjectCard({ project }: { project: typeof projects[number] }) { const [hover, setHover] = useState(false); return <Reveal><article className="group relative overflow-hidden rounded-xl border border-zinc-700/50 bg-zinc-800/50 backdrop-blur-sm transition hover:border-purple-500/50" onMouseEnter={() => setHover(true)} onMouseLeave={() => setHover(false)}><div className="relative h-48 overflow-hidden"><img src="/placeholder.svg?height=400&width=600" alt={project[0]} className={`h-full w-full object-cover transition duration-700 ${hover ? "scale-110" : ""}`} /><div className="absolute inset-0 bg-gradient-to-b from-purple-500/20 to-pink-500/20 opacity-0 transition group-hover:opacity-100" /></div><div className="p-6"><h3 className="mb-2 text-xl font-bold">{project[0]}</h3><p className="mb-4 text-zinc-400">{project[1]}</p><div className="mb-6 flex flex-wrap gap-2">{project[2].map(tag => <span key={tag} className="rounded-md bg-zinc-700/50 px-2 py-1 text-xs text-zinc-300">{tag}</span>)}</div><div className="flex justify-between border-t border-zinc-700/50 pt-4"><a href="https://github.com" className="flex items-center text-sm text-zinc-400 hover:text-white"><Github className="mr-2 h-4 w-4" />Code</a><a href="https://example.com" className="flex items-center rounded-md bg-gradient-to-r from-purple-500 to-pink-500 px-3 py-2 text-sm">Live Demo <ArrowUpRight className="ml-2 h-4 w-4" /></a></div></div></article></Reveal> }

function ContactForm() { const [sending, setSending] = useState(false); const [sent, setSent] = useState(false); const submit = async (e: FormEvent<HTMLFormElement>) => { e.preventDefault(); setSending(true); await new Promise(r => setTimeout(r, 1000)); setSending(false); setSent(true); e.currentTarget.reset() }; return <Reveal><div className="rounded-xl border border-zinc-700/50 bg-zinc-800/50 p-6"><h3 className="mb-6 text-2xl font-bold">Send Me a Message</h3><form onSubmit={submit} className="space-y-4"><input required placeholder="Your Name" className="w-full rounded-md border border-zinc-700 bg-zinc-900/50 px-3 py-2 outline-none focus:border-purple-500" /><input required type="email" placeholder="Your Email" className="w-full rounded-md border border-zinc-700 bg-zinc-900/50 px-3 py-2 outline-none focus:border-purple-500" /><input required placeholder="Subject" className="w-full rounded-md border border-zinc-700 bg-zinc-900/50 px-3 py-2 outline-none focus:border-purple-500" /><textarea required rows={5} placeholder="Your Message" className="w-full rounded-md border border-zinc-700 bg-zinc-900/50 px-3 py-2 outline-none focus:border-purple-500" /><button disabled={sending} className="flex w-full items-center justify-center rounded-md bg-gradient-to-r from-purple-500 to-pink-500 px-4 py-2 font-medium disabled:opacity-60">{sending ? "Sending..." : sent ? "Message sent" : <>Send Message <Send className="ml-2 h-4 w-4" /></>}</button></form></div></Reveal> }

export default function Portfolio() { return <main className="min-h-screen overflow-hidden bg-gradient-to-b from-zinc-900 via-zinc-900 to-black text-white"><MouseFollower /><ScrollProgress /><FloatingNav /><section className="relative flex min-h-screen items-center justify-center"><div className="container relative z-10 grid items-center gap-12 lg:grid-cols-2"><div className="space-y-6"><span className="inline-block rounded-full border border-white/20 bg-white/10 px-3 py-1 text-sm">Software Engineer & Creative Developer</span><h1 className="text-5xl font-bold tracking-tight md:text-7xl">Hi, I&apos;m <span className="block bg-gradient-to-r from-purple-400 to-pink-600 bg-clip-text text-transparent">Shine Kyaw Kyaw Aung</span></h1><p className="max-w-[600px] text-xl text-zinc-400">I craft exceptional digital experiences with code, creativity, and a passion for innovation.</p><div className="flex flex-wrap gap-4"><a href="#projects" className="flex items-center rounded-md bg-gradient-to-r from-purple-500 to-pink-500 px-4 py-2">View Projects <ArrowRight className="ml-2 h-4 w-4" /></a><a href="#contact" className="rounded-md border border-zinc-700 px-4 py-2 text-pink-500">Contact Me</a></div><div className="flex gap-4">{[[Github, "https://github.com/shinekyaw"], [Linkedin, "https://www.linkedin.com/in/shinekyawkyawaung/"], [Mail, "mailto:shinekyawkyawaung@gmail.com"]].map(([Icon, href]) => <a key={href as string} href={href as string} className="rounded-full bg-zinc-800/50 p-3 text-zinc-400 hover:text-white"><Icon className="h-5 w-5" /></a>)}</div></div><ParticleHero /></div></section><section id="about" className="container py-32"><Heading title="About Me" subtitle="My background and journey" /><div className="mt-16 grid items-center gap-12 md:grid-cols-2"><img src="/placeholder.svg?height=600&width=600" alt="Shine Kyaw Kyaw Aung" className="aspect-square rounded-xl border border-zinc-800 object-cover" /><Glass><p className="text-lg text-zinc-300">I&apos;m a passionate software engineer with experience building web applications and digital products. I specialize in frontend development with React and Next.js, but I&apos;m also comfortable working with backend technologies.</p><p className="mt-4 text-lg text-zinc-300">My journey in tech started with a strong foundation in software development. I&apos;ve worked with various companies to create intuitive, performant, and accessible digital experiences.</p><div className="mt-8 grid grid-cols-2 gap-4 text-sm"><div><span className="text-zinc-500">Name</span><br />Shine Kyaw Kyaw Aung</div><div><span className="text-zinc-500">Email</span><br />shinekyawkyawaung@gmail.com</div><div><span className="text-zinc-500">Location</span><br />Myanmar</div><div><span className="text-zinc-500">Availability</span><br /><span className="text-green-500">Open to opportunities</span></div></div></Glass></div></section><section id="skills" className="container py-32"><Heading title="My Skills" subtitle="Technologies I work with" /><div className="mt-16 grid grid-cols-2 gap-6 md:grid-cols-3 lg:grid-cols-4">{skills.map(([name, level]) => <Reveal key={name as string}><div className="rounded-xl border border-zinc-700/50 bg-zinc-800/50 p-6"><div className="mb-4 text-center font-medium">{name}</div><div className="h-2.5 overflow-hidden rounded-full bg-zinc-700"><motion.div className="h-full rounded-full bg-gradient-to-r from-purple-500 to-pink-500" initial={{ width: 0 }} whileInView={{ width: `${level}%` }} viewport={{ once: true }} /></div><div className="mt-2 text-right text-sm text-zinc-400">{level}%</div></div></Reveal>)}</div></section><section id="projects" className="container py-32"><Heading title="Featured Projects" subtitle="Some of my recent work" /><div className="mt-16 grid gap-8 md:grid-cols-2 lg:grid-cols-3">{projects.map(project => <ProjectCard key={project[0]} project={project} />)}</div></section><section id="experience" className="container py-32"><Heading title="Work Experience" subtitle="My professional journey" /><div className="mx-auto mt-16 max-w-4xl space-y-8">{experiences.map((experience, i) => <Reveal key={experience[0]}><div className="rounded-xl border border-zinc-700/50 bg-zinc-800/50 p-6"><h3 className="text-xl font-bold">{experience[0]}</h3><div className="mb-4 text-zinc-400">{experience[1]} | {experience[2]}</div><p className="text-zinc-300">{experience[3]}</p></div></Reveal>)}</div></section><section id="contact" className="container py-32"><Heading title="Get In Touch" subtitle="Let&apos;s work together" /><div className="mt-16 grid gap-12 md:grid-cols-2"><Glass><h3 className="mb-6 text-2xl font-bold">Contact Information</h3><div className="space-y-5 text-zinc-300"><p className="flex items-center gap-3"><Mail className="text-purple-400" />shinekyawkyawaung@gmail.com</p><p className="flex items-center gap-3"><Linkedin className="text-purple-400" />linkedin.com/in/shinekyawkyawaung</p><p className="flex items-center gap-3"><Github className="text-purple-400" />github.com/shinekyaw</p></div></Glass><ContactForm /></div></section><footer className="border-t border-zinc-800 py-12"><div className="container flex flex-col items-center justify-between gap-6 md:flex-row"><div><span className="text-xl font-bold"><span className="bg-gradient-to-r from-purple-400 to-pink-600 bg-clip-text text-transparent">Shine</span>KKA</span><p className="mt-2 text-sm text-zinc-500">© {new Date().getFullYear()} Shine Kyaw Kyaw Aung. All rights reserved.</p></div></div></footer></main> }

// All portfolio UI, interaction effects, canvas animation, navigation, cards, timeline, and contact form live in this file.
